# 裂缝检测商业化路线与差距分析

## Context（为什么写这份文档）

本文件回答三个问题:(1) 裂缝检测的商业工业用途应该是什么样;(2) 要把这个项目做成严格的商业工业项目需要做什么;(3) Qwen-VL 在本项目能否派上用场。

基于对 `/home/alientek/yolo8-seg` 仓库的完整盘点 + 实际代码阅读(`rknn_seg_zc.cpp` 686 行、`post_cut.py`、各测试脚本),下面给出严格、诚实的分析。**核心结论先行**:当前仓库是一个"NPU 推理性能基准原型",离商业工业产品差距很大,且差距不在"速度"而在"可信度、数据、工程化、集成、合规"。速度已经做到位了,接下来的钱和精力几乎全在别处。

---

## 第一部分：裂缝检测的商业工业用途应该是什么样

### 1.1 真实市场与场景

裂缝检测(表面缺陷检测)在工业界是一个**成熟且拥挤**的赛道,不是新蓝海。真实的商业落地点按价值/可行性排序:

**① 桥梁/道路/隧道/边坡巡检(最大、最契合当前 demo)**
- 形态:车载/无人机/定点摄像头,周期性巡检,产出"缺陷台账 + 严重程度分级 + 修复优先级"。
- 痛点:人工巡检慢、主观、危险(高空/封闭车道);需要可追溯的带 GPS/时间戳的记录。
- 商业模式:政府/交通部门的检测服务外包,或卖给检测机构做工具。

**② 工厂产线质量检测(混凝土预制件/铸件/焊缝/涂层)**
- 形态:固定工位、稳定光照、高吞吐(每件几秒),与 MES/质检流程对接,自动判合格/不合格。
- 痛点:要求极高一致性、可重复、低误报(误报导致漏检被罚款,误报导致返工成本)。
- 商业模式:产线集成,按工位/按年服务。

**③ 管道/储罐/压力容器(强监管)**
- 涉及防爆认证、特种相机(内窥/爬行机器人)、法定定期检测。门槛高、利润高、周期长。

**④ 建筑/幕墙/外墙渗漏**
- 立面爬检/无人机;裂缝常与渗水、钢筋锈蚀关联,需要多源融合与几何标定。

### 1.2 商业产品"应该长什么样"（诚实标准）

一个真正的商业裂缝检测产品,不是"一个能跑的模型",而是:

1. **可量化的精度承诺**:在带标注的、代表真实分布的数据集上,mAP / mask-IoU / 召回率有数字,且按缺陷等级分桶报告。不是"我们检测得准",而是"在 XX 数据集上 mAP50=0.xx,漏检率<0.x%"。
2. **可追溯、可审计**:每一条检测记录带时间/GPS/设备ID/原图/掩码/置信度/操作员,落库可查,符合检测报告存档要求。
3. **稳健到能现场用**:户外光照(强光/阴影/潮湿/夜间)、不同表面材质、运动模糊、相机抖动下不崩。有明确的失效边界和降级策略(检测置信度低时告警让人复核,而不是硬判)。
4. **与现有流程集成**:输出能进客户的 MES/SCADA/巡检系统,而不是只能在板子上 imshow。
5. **可部署、可运维**:一键安装/升级、远程监控、故障自恢复、日志可诊断、模型可灰度替换。
6. **合规**:行业认证(交通检测资质、防爆、CE/FCC、数据合规),以及检测报告的法定格式。

---

## 第二部分：当前项目的真实定位与差距（基于盘点）

### 2.1 现状定性

当前仓库 = **一个把 YOLOv8n-seg INT8 在 RK3588 NPU 上压到 16.3ms/61FPS 的优秀性能基准**,外加若干一次性测试脚本。它回答了"能不能快",没有回答"能不能商用"。

### 2.2 具体差距（盘点实测，非估计）

| 维度 | 现状 | 商业要求 | 差距 |
|------|------|---------|------|
| **精度评测** | 无。仅单图 conf=0.349 对标 Python 参考 | 带标注集 mAP/mask-IoU/召回 | **致命** |
| **标注数据集** | 无。images/ 49 张量化校准图无标签,val/ 10 张无标签 | train/val/test + labels + data.yaml | **致命** |
| **模型多类** | 单类 crack,cls 通道=1 硬编码 | 至少区分 裂缝/渗水/剥落/坑洞 | 大 |
| **阈值** | 4 套脚本 4 套阈值(0.18/0.25/0.15…),C++ 里 static const 不可运行时调 | 统一配置,可调,可校准 | 大 |
| **配置系统** | 无 yaml/json/toml/argparse,全靠改源码常量 | 外置 config + 热加载 | 大 |
| **日志** | printf/print,无 logger、无落盘日志 | 结构化日志 + 滚动 + 远程 | 大 |
| **错误处理/容错** | rknn 调用失败直接 return -1;无重试/看门狗/降级/兜底模型 | 失败可恢复、可降级、有看门狗 | 大 |
| **部署打包** | 无 Dockerfile/systemd/.deb/installer/Makefile | 一键部署、systemd 自启、OTA | 大 |
| **CI/CD** | 非 git 仓库,无任何 CI | 自动构建/测试/模型回归 | 大 |
| **文档** | 有研究报告+板端说明,无 README | README + 运维手册 + API 文档 | 中 |
| **模型版本/校验** | 8 个 .rknn 散落命名,无 checksum/manifest | 版本号 + sha256 + 注册表 | 中 |
| **相机标定** | 无内参/畸变,无去畸变 | 几何测量需标定 | 视用途 |
| **网络输出** | 无 RTSP/MQTT/HTTP/gRPC | 检测结果需上报 | 大 |
| **持久化** | 无 DB,结果不落库 | 检测事件库(SQLite/时序) | 大 |
| **记录/回放** | video_cut.py 能存视频,其余只 imshow | 缺陷台账 + 截图/片段归档 | 大 |
| **热稳定性** | 未做长时间运行/热降频监控 | 7×24 稳定性 + 温度监控 | 中 |

**最危险的一句总结**:报告强调的"精度无损"指的是"INT8 cut-tail 与 FP16 参考在单张图上 conf 一致"——这只证明量化没破坏数值,不证明模型在真实裂缝上检测能力强。商业上第一个被问的就是"你的召回率多少、漏检率多少",现在答不出来。

### 2.3 仓库现状细节（盘点实测）

- **文件构成**:C++ 源 `rknn_seg_zc.cpp`(686 行,主交付物)+ `probe_zero_copy.cpp`;Python 含 `post_cut.py`(numpy 参考后处理)、`convert_cut.py`(INT8 转换)、`vtes_cut.py`/`video_cut.py`/`live_cut.py`/`live_bench.py`/`inferi.py` 等一次性测试脚本;模型 8 个 `.rknn` + 多个 `.onnx`/`.pt`;`板端部署说明.md` + 研究报告;两张测试图 `test.jpg`/`val/*.jpg`(无标签)。
- **阈值分裂**:四套脚本阈值各不相同 —— `post_cut.py` 用 `0.18/0.45/0.5`;`inferi.py`/`video.py` 用 `0.15/0.60/0.2`;`vtes.py` 用 `0.25/0.50/0.3`;C++ `rknn_seg_zc.cpp` 用 `0.18/0.45/0.5` 且为 `static const` 不可运行时改。
- **唯一运行时配置项**:C++ 里只有 `CORE_MASK` 环境变量(`rknn_seg_zc.cpp:109-118`)可运行时调,其余全编译期常量。
- **无网络/无 DB/无 CI/非 git**:全仓库 grep 不到 rtsp/mqtt/grpc/sqlite;无 `.git`、无 CI 配置;部署靠 `板端部署说明.md` 里手写 adb push 三步。

---

## 第三部分：做成严格商业工业项目需要做什么（按优先级）

### P0 — 不做就不能谈商业（可信度基座）

1. **建标注数据集 + train/val/test 划分**
   - 收集真实巡检/产线图像(数百~数千张,覆盖多场景/光照/材质),YOLO-seg 格式标注(框 + 多边形掩码),`data.yaml` 划分。
   - 现有 `val/` 10 张、`images/` 49 张无标签,需从零标注或采购公开裂缝数据集(Crack500/CRACK500、DeepCrack 等)做预训练 + 微调。

2. **精度评测管线(eval.py)**
   - 在 val/test 上算 mAP50/mAP50-95、mask-IoU、per-class 召回/精确、漏检率、误报率,按裂缝宽度/严重程度分桶。
   - 建立回归基线:每次模型/阈值改动后跑一遍,数字掉了就挡回去。这是商业质量的唯一硬证据。

3. **召回优先的阈值校准**
   - 裂缝检测是"漏检代价远大于误检"的安全场景。阈值应调到高召回,再用后续环节(人工复核/VLM)压误报,而不是图省事取 0.18。

### P1 — 工程化基座（让它能被运维、被集成）

4. **统一配置系统(config.yaml + 热加载)**
   - 阈值、模型路径、相机参数、输出开关、NPU core、日志级别全部外置。消灭 4 套脚本 4 套阈值。
   - C++ 侧去掉 `static const`,改为启动读 config + 支持运行时 reload。

5. **结构化日志 + 监控**
   - spdlog(结构化 JSON 日志、滚动、级别),关键字段:帧ID/时间/检测数/置信度/延迟/NPU负载/温度。
   - 暴露 metrics 端点(Prometheus)供远程看板。

6. **容错与降级**
   - rknn 调用失败 → 重试 N 次 → 切兜底 FP16 模型 → 仍失败则进入"只采集存证、告警人工"降级模式,绝不静默崩溃。
   - systemd watchdog + 自动重启;采集与推理解耦,采集断了不影响推理上一次结果上报。

7. **部署打包**
   - CMakeLists/Makefile 正式构建;systemd service 自启;交叉编译产物 + 一键 install 脚本;模型 sha256 校验 + 版本 manifest;OTA/远程升级通道。

### P2 — 集成与产品化（让它能进客户系统）

8. **检测事件持久化(SQLite/时序库)**
   - 每条检测:时间戳/设备ID/GPS(可选)/原图路径/掩码路径/置信度/严重等级/复核状态。可查询、可导出。

9. **网络上报(MQTT/HTTP/gRPC + 可选 RTSP 推流)**
   - 检测事件 MQTT 上报云端看板;可选 RTSP 把 annotated 视频流推出去供远端查看。

10. **检测报告生成**
    - 按巡检任务批量导出标准报告(PDF/Excel):缺陷位置图、掩码叠加图、严重程度、建议措施、签章位。这是巡检交付物。

11. **多缺陷类别扩展**
    - 从单类 crack 扩到 裂缝/渗水/剥落/坑洞/钢筋外露 等;cls 通道 >1 路径在 `rknn_seg_zc.cpp:216` 已预留,需补模型与标注。

12. **相机标定与几何测量(按用途)**
    - 需要量裂缝宽度/长度时,做相机内参标定 + 去畸变 + 像素→物理尺寸换算,输出"裂缝宽 X mm"。

### P3 — 稳健性与合规（长期可商用）

13. **长时间稳定性测试**:7×24 运行,NPU 负载/温度/内存/掉帧率监控,确认无热降频、无内存泄漏。
14. **对抗/边界用例集**:强光、夜间、潮湿、模糊、污渍伪装裂缝、无裂缝背景,测假阳/假阴。
15. **合规**:行业资质(交通检测)、防爆(危险场景)、CE/FCC(电子产品)、数据合规(图像存留策略)。
16. **CI/CD + 模型注册表**:纳入 git,自动构建 + 回归评测,模型版本化,生产模型可追溯可回滚。

---

## 第四部分：Qwen-VL 在本项目能否派上用场（诚实评估）

**能,但角色必须是"触发式的语义增强层",绝不是逐帧跑。** 而且要清醒:在 RK3588 上 Qwen-VL 是重的、慢的,用得不好是累赘。

### 4.1 真正有价值的用法（按性价比排序）

**① 自动严重程度分级与报告生成（最高价值）**
- YOLOv8-seg 给出裂缝的精确几何(框/掩码/像素面积/长度/宽度估算),Qwen-VL 看"裁剪后的缺陷图 + 检测标注",输出结构化结论:类型(横向/纵向/网状/龟裂)、严重程度(轻/中/重)、是否活跃裂缝、建议(继续观察/限期修复/立即处置)。
- 这直接产出巡检交付物的"判断"部分,是纯 seg 模型给不了的语义。**触发式调用**:每条新检测调一次,不影响 30FPS 前端。

**② 误报过滤与复核（降低人工复核成本）**
- seg 模型难免把污渍、阴影接缝、伸缩缝误判为裂缝。把"置信度 0.18–0.35 灰区"的检测送给 Qwen-VL 二次判定"是裂缝还是非裂缝",能显著压误报。这是商业上"低误报"承诺的关键杠杆之一。

**③ 多缺陷语义识别（弥补单类）**
- 当前模型只认 crack。Qwen-VL 可在不重训 seg 的情况下,识别画面里的渗水、剥落、钢筋外露、植物侵入等,作为"补充缺陷清单"。精度不如专门模型,但上市快。

**④ 交互式巡检对话**
- 巡检员对某条裂缝追问"这条和上次比变长了吗/需要现在修吗",Qwen-VL 结合历史记录与当前图给出自然语言答复。提升产品体验与粘性。

### 4.2 不该用它做的事（避免踩坑）

- **不要逐帧跑**:Qwen-VL-2B 量化版在 RK3588 上单次推理百 ms 至秒级,塞进 30FPS 管线会直接拖垮。必须触发式、异步、独立线程/进程。
- **不要让它替代 seg 做定位/分割**:VLM 的 grounding 精度远不如专门分割模型,且更慢更贵。定位/掩码交给 YOLOv8-seg,VLM 只做"看懂+判断+生成文字"。
- **不要每条检测都无条件调**:低置信度/无变化/历史已确认的,跳过 VLM,省算力。

### 4.3 落地架构（与现有零拷贝前端衔接）

```
[相机30FPS] → [YOLOv8-seg 零拷贝前端(本系统)] → 检测流(框/掩码/conf)
                                                    │
                                          触发(新缺陷/灰区/定时)
                                                    ▼
                            [Qwen-VL 语义层] 独立进程,异步,不阻塞前端
                              ├─ 输入:裁剪帧 + seg 标注文本(grounding)
                              ├─ 输出:类型/严重度/建议/是否误报
                              └─ 结果落库 + 进报告
                                                    │
                                                    ▼
                                          [云端看板/报告/告警]
```

关键复用点:本项目的零拷贝封装(`rknn_create_mem`/`set_io_mem`/`mem_sync`)、NEON 后处理、performance 调速器、异步采集流水线,Qwen-VL 的视觉编码器若走 RKNN 可直接复用。检测结果作 VLM prompt 的 grounding(把"320×58 像素裂缝,conf 0.347"转文本喂进去)比让 VLM 自己找更准更省 token。共享前端已持有的最新帧,不为 VLM 再开一个相机句柄。

### 4.4 Qwen-VL 自身的诚实风险

- RK3588 跑 Qwen2-VL-2B INT4 需 ~1.5–2GB 权重 + KV cache,内存够但与 seg 争 NPU/CPU;建议 VLM 的 LLM 部分绑特定 CPU 核(taskset)或评估能否部分上 NPU。
- VLM 输出不稳定/幻觉,关键判断(严重程度)仍需可追溯规则或人工复核兜底,不能全权交给 VLM。
- 模型更新、prompt 版本管理要做,否则不可复现。

---

## 第五部分：总结与建议的推进顺序

**诚实定位**:这个项目目前是一个"推理性能做到工业领先水平的原型"。性能这一关已经过了,但商业产品的战场在别处——数据与精度证据、工程化、集成、合规。

**建议顺序**:
1. **先补 P0(数据集 + 评测管线)**。没有 mAP 数字,一切商业洽谈都站不住。这是当前最大短板,也是性价比最高的投入。
2. **再做 P1(配置/日志/容错/部署)**,让它从一个脚本变成一个可运维的服务。
3. **然后 P2(持久化/网络/报告/多类/标定)**,接进客户系统,变出可交付物。
4. **Qwen-VL 在 P2 报告生成与误报过滤环节切入**,触发式、异步,复用零拷贝前端。不要早插,否则会被 VLM 的工程复杂度拖慢主线。
5. **P3 稳健性与合规**伴随长期打磨。

**一句话**:速度是亮点,可信度是缺口。先证明它"测得准",再让它"能交付、能运维、能合规",Qwen-VL 作为语义增强层在交付物环节接入——这是把当前原型做成商业裂缝检测产品最务实的路径。

---

## 落地标志（如何检验各阶段是否到位）

- **P0 落地标志**:能在 test 集跑出 mAP50/mask-IoU/召回/漏检数字,且每次模型改动有回归对比。
- **P1 落地标志**:`systemctl start` 后 7×24 不崩,日志可查,config 改阈值不重编译。
- **P2 落地标志**:一条裂缝从检测到生成带图的 PDF 报告,全程自动化,可远端查看。
- **Qwen-VL 落地标志**:前端 30FPS 不掉帧的同时,VLM 对灰区检测输出结构化严重度,人工复核量下降可量化。

---

*本文件为分析与路线规划,不含代码改动。当前性能状态见同目录《研究报告_YOLOv8-seg_RK3588零拷贝优化.md》。*
