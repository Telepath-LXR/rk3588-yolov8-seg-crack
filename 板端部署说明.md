# YOLOv8-seg INT8 裂缝检测 — 板端部署使用说明

## 背景一句话
原 ONNX 端到端模型做 INT8 量化后,板端置信度全 0,根因是 INT8 把尾部算子(DFL/Softmax/Sigmoid/框解码)量化塌掉了。  
解决: 把 ONNX 切成 10 个原始 Conv 分支输出(`yolo8n_cut.onnx`),只让 Conv 走 INT8,DFL/Sigmoid/解码全部挪到 numpy 后处理。FP16 不受影响但 INT8 这样切完就不再塌了。

## 需要拷到板子的 3 个文件(都在 /home/alientek/yolo8-seg/)
- `yolo8n_int8_cut.rknn` —— INT8 模型(4.5MB,板端运行)
- `post_cut.py` —— 单类后处理库(DFL/解码/掩码,纯 numpy+cv2)
- `vtes_cut.py` —— 板端摄像头测试脚本(含可视化存图)

## 部署步骤(板子端, Python 3.11, 自带 rknnlite)

```bash
# 1. 在 PC 上把 3 个文件推到板子 /yolo8 (adb 已连接)
adb push /home/alientek/yolo8-seg/yolo8n_int8_cut.rknn /yolo8/
adb push /home/alientek/yolo8-seg/post_cut.py            /yolo8/
adb push /home/alientek/yolo8-seg/vtes_cut.py            /yolo8/

# 2. 进入板子并运行
adb shell
cd /yolo8
python3 vtes_cut.py
```

## 怎么算成功
- 控制台每帧打印 `检测 N 个, 最高分 0.xx, 掩码像素 YYY`(不再是 0.000)
- 首帧会先打印 10 个输出的 dtype/shape/min/max,用于核对
- 检测到第一帧时自动存 `cut_board.png`(红掩码+绿框)
- 最后打印平均推理/后处理毫秒数和端到端 FPS

## 把可视化图拉回 PC 看
```bash
# PC 上执行
adb pull /yolo8/cut_board.png /home/alientek/yolo8-seg/
```

## 常见问题

**Q: 板端还是全 0.000?**  
A: 先看首帧打印的 `out[1]`(cls0)/`out[4]`(cls1)/`out[7]`(cls2) 的 min/max。
- 若 max 还是个正常负数(比如 -0.9 ~ 0.3),说明模型正常,后处理阈值 0.25 偏高,改 `post_cut.py` 顶部 `OBJ_THRESH=0.15`。
- 若 max 是 -127 之类极端值,说明板端返回的是 int8 原始值没反量化 → 见下条。

**Q: 板端输出是 int8 不是 float32?**  
A: `vtes_cut.py` 首帧已打印 dtype。`post_cut.py` 开头已加 `np.asarray(o, dtype=np.float32)`,对 float32 是无操作、对 int8 也能跑通不崩。但若 int8 的 min/max 在 -127~127 区间且值域不对,需手动反量化:
在 `post_cut.py` 的 `outputs = [np.asarray(...)]` 那行换成:
```python
# 仅当 dtype 是 int8 且值域明显是量化整数时启用
import numpy as np
def _dq(o):
    o = np.asarray(o, dtype=np.float32)
    if o.dtype == np.int8 or np.abs(o).max() > 1.5:  # 粗判
        scale = 1.0   # TODO: 用 rknn.get_output_attrs 在转换时拿 scale/zero
        zp = 0.0
        o = (o - zp) * scale
    return o
outputs = [_dq(o) for o in outputs]
```
> 注: 当前模型 build 时 toolkit2 会把输出默认设成 int8 以提速,但板端 RKNNLite.inference() 通常已自动反量化成 float32(和模拟器一致)。先按默认 float32 跑,出问题再处理。

**Q: 摄像头打不开 /dev/video44?**  
A: 板子上 `ls /dev/video*` 看可用节点,改 `vtes_cut.py` 顶部 `CAMERA_DEV`。

**Q: 想测静态图而不是摄像头?**  
A: 把 `vtes_cut.py` 里 `cap.read()` 换成 `cv2.imread('xxx.jpg')` 即可。

**Q: 想改置信度/掩码阈值?**  
A: 改 `post_cut.py` 顶部三个常量:
```python
OBJ_THRESH = 0.25    # 框置信度阈值(调低=更多框)
NMS_THRESH  = 0.45   # NMS IoU
MASK_THRESH = 0.5    # 掩码二值化阈值
```

## 文件对应关系(别拷错)
| 板端用途 | 文件 | 大小 |
|---|---|---|
| INT8 模型 | yolo8n_int8_cut.rknn | 4.5MB |
| 后处理库 | post_cut.py | ~6KB |
| 测试脚本 | vtes_cut.py | ~3KB |

> ⚠️ 别和旧的 `yolo8n_int8_hybrid.rknn`(方案B,已验证板端全0失败)或 `yolo8n_int8.rknn`(端到端INT8,同样全0)搞混。这次用的是带 `_cut` 后缀的。
