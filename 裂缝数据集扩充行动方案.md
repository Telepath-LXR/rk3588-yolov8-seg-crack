# 裂缝数据集扩充行动方案（3000 → 4500，发给 PC 端 Claude Code 执行）

## 背景

- 当前数据集：约 3000 张，yolov8n-seg，单类 crack，YOLO-seg 格式（polygon 标注）。
- 目标：从 Roboflow 补充 1500-2000 张**同类、同分布**的裂缝图，凑到 4000-5000 张，提升模型泛化。
- 场景：裂缝（墙体/路面/混凝土表面），实例分割。
- **关键原则**：混数据前先看分布是否接近，加完一批跑 val，mAP 降了就退回。不盲目堆量。

---

## 第一步：盘点现有数据集（搞清"同分布"是什么）

请先告诉我（或自己确认）当前 3000 张数据集的属性，这是后续判断"新数据是否同类"的基准：

1. 数据集在哪个路径？`data.yaml` 里 `train`/`val`/`path` 指向哪？
2. 类别：`nc` 和 `names`（确认是单类 crack）。
3. 抽 5-10 张 train 图看一眼：裂缝是**墙体/路面/混凝土**哪种？**近景(裂缝占画面大)还是远景(细线)**？光照、分辨率大致什么样？
4. 抽 5-10 个 label 文件，确认格式是 YOLO-seg polygon：
   ```
   <class_id> <x1> <y1> <x2> <y2> ... <xn> <yn>
   ```
   （归一化到 0-1 的多边形点集，不是 bbox 4 个值，也不是 RLE）

把上面这些信息记下来，作为"目标分布"。后面每批新数据都要和它比。

---

## 第二步：在 Roboflow Universe 上找候选数据集

### 怎么找

1. 浏览器打开 https://universe.roboflow.com
2. 搜索关键词（按顺序试，找到合适的就停）：
   - `crack segmentation`
   - `concrete crack instance segmentation`
   - `pavement crack`
   - `wall crack`
   - `surface defect crack`
3. 筛选条件（重要）：
   - **Project Type = Instance Segmentation**（不是 Classification，也不是 Semantic Segmentation）
   - **Class 数 = 1**（只认 crack 一类，别下多类的，除非你确定要做多类）
   - 图量 > 500（太小的没价值）

### 怎么判断一个候选"好不好"（下载前的预筛）

对每个候选数据集页面，看：
1. **Class** 标签里是 `crack` / `Crack` / `裂缝`，且只有一个类。
2. **图片样例**（页面会显示预览图）：裂缝形态、场景、分辨率是否和你现有 3000 张接近。**接近就优先，差太远就跳过。** 比如你的是墙面近景裂缝，就别下路面远景细裂缝集。
3. 图量：1000-3000 的最合适，太大（上万）的要警惕质量。
4. 看 "Download" 里支持的格式，确认有 **YOLOv8** 格式（Roboflow 的 YOLOv8 格式对 seg 模型导出的是 polygon）。

### 建议找 2-3 个候选，每个先小下载试

Roboflow 每个 dataset 可以下 "Download" → 选 YOLOv8 格式 → "download zip to computer"。
**先下 1 个候选的全部，先看质量再决定要不要全量。**

---

## 第三步：下载与格式检查

### 3.1 准备工作目录

在 PC 上建一个工作区（不要直接动原 3000 张的数据集目录，先在副本上操作）：

```bash
# 假设你的原始数据集在 ~/crack_original/
mkdir -p ~/crack_merge/{images/train,images/val,labels/train,labels/val}
cp -r ~/crack_original/* ~/crack_merge/   # 先复制一份原始的进来做基底
```

### 3.2 下载 Roboflow 数据集

Roboflow 下载页会给一段下载代码（需要 API key，免费注册即有），类似：

```bash
# Roboflow 给的下载片段（替换为你实际的）
pip install roboflow
```

```python
from roboflow import Roboflow
rf = Roboflow(api_key="你的API_KEY")
project = rf.workspace("xxx").project("crack-xxx")
version = project.version(1)
dataset = version.download("yolov8")
```

**注意**：Roboflow 下载的 YOLOv8 格式结构通常是：
```
<dataset_name>/
  data.yaml
  train/images/*.jpg
  train/labels/*.txt
  valid/images/*.jpg
  valid/labels/*.txt
  test/images/*.jpg
  test/labels/*.txt
```
它的 `data.yaml` 用的是相对路径 `train/images` 这种，要改成绝对路径或调整。

### 3.3 下载后必做的格式核验（防止踩坑）

下载解压后，立刻抽查，不要直接合：

```bash
# 1. 看类别，确认是单类 crack
cat data.yaml

# 2. 抽查 label 格式是不是 polygon（不是 bbox）
#    polygon 格式：行里有很多个数对（>4 个数），归一化 0-1
#    bbox 格式：只有 class + 4 个数
head -n 2 train/labels/$(ls train/labels | head -1)
# 如果输出像 "0 0.512 0.343 0.551 0.350 ..."（很多点）= polygon ✓
# 如果输出像 "0 0.5 0.5 0.2 0.3"（只有4个归一化数）= bbox ✗，不能用
```

```bash
# 3. 数量核对
echo "train images: $(ls train/images | wc -l)"
echo "train labels: $(ls train/labels | wc -l)"
echo "val images: $(ls valid/images | wc -l)"
# images 和 labels 数量应一致，差太多说明有图无标注

# 4. 随机看几张图 + 标注重叠，肉眼确认标注贴边、没乱标
#    （这一步可以用 ultralytics 的可视化，或写个小脚本）
```

**如果发现**：label 是 bbox 不是 polygon → 这个集不能用于 seg，换一个候选。
**如果发现**：大量图无 label → 跳过，质量太差。
**如果发现**：polygon 点稀疏（每条裂缝只有 4-6 个点，画出来是直线）→ 标注质量低，能凑数但 Mask 上限会被它拖累，慎用。

---

## 第四步：小批试混 + 验证集守门（最关键的一步）

**不要一次性把新数据全合进 train。先小批混，跑 val 看是升是降。**

### 4.1 只混一部分先看趋势

从 Roboflow 数据集里取 **1/3**（比如下的是 1500 张，先只混 500 张进 train），合进基底：

```bash
# 假设 Roboflow 下载的 train 在 ~/roboflow_crack/train/
# 取前 500 张
ls ~/roboflow_crack/train/images | head -500 > /tmp/to_add.txt
# 复制图和对应 label 到合并区
while read f; do
  cp ~/roboflow_crack/train/images/"$f" ~/crack_merge/images/train/
  base="${f%.*}"
  cp ~/roboflow_crack/train/labels/"$base".txt ~/crack_merge/labels/train/ 2>/dev/null
done < /tmp/to_add.txt
```

**关键：val 集不要动！val 保持原始 3000 张的分布**，这样 val mAP 才能反映"新数据有没有帮到你真实关心的分布"。

### 4.2 训练一个对照版本

用和原模型**完全相同**的超参训练（imgsz=640, batch=8, epochs 先设少一点如 100 看 early trend）：

```bash
yolo segment train data=~/crack_merge/data.yaml model=yolov8n-seg.pt epochs=100 imgsz=640 batch=8 device=0 name=seg_merge_v1
```

### 4.3 看趋势决策（守门）

训练完看 val mAP，和原 3000 张的 baseline（Box 0.83 / Mask 0.67）比：

| 结果 | 含义 | 决策 |
|------|------|------|
| Box 和 Mask 都升 | 新数据分布接近、有用 | 把剩下的 2/3 也混进来，再训 |
| Box 升 Mask 几乎不变 | 检测变好但分割没动 | 可以混，但别指望 Mask 大涨 |
| **任一指标降** | **分布太远，新数据帮倒忙** | **退回，不要这批，换一个候选** |
| 持平 | 数据没害也没大用 | 可混可不混，看你时间 |

**只有"升或持平"才继续加。"降了就退"是铁律。**

如果第一批 500 张是升的，再把剩下 1000 张全混进 train，重训，看是否进一步升。

### 4.4 退回机制

如果第一批就降了，不要纠结这个数据集，回到第二步换下一个 Roboflow 候选。**不同 Roboflow crack 集分布差异很大，试 2-3 个很正常。**

---

## 第五步：确定最终数据集 + 重训

第一批验证通过、决定全量混入后：

1. 把验证过有用的 Roboflow 数据全部并入 train（val 仍保持原始分布）。
2. **检查图片尺寸**：如果 Roboflow 的图分辨率和原始差很多（比如一个是 4K 一个是 512），训练前统一 resize 到相近量级（如 1280 长边），或确认 ultralytics 的 letterbox 能处理。YOLOv8 默认会 letterbox 到 640，问题不大，但极端分辨率差会影响。
3. **类别 id 统一**：确认 Roboflow 的 class 0 也是 crack。如果它的 data.yaml 里 crack 是 class 0，直接合；如果不是，要把 label 里第一个数改对。
4. 重训完整 epochs（200，patience=100，和原配置一致）：

```bash
yolo segment train data=~/crack_merge/final_data.yaml model=yolov8n-seg.pt epochs=200 patience=100 imgsz=640 batch=8 device=0 name=seg_4500
```

5. 训练完看 `results.csv` 的最终 val mAP，和原 baseline 比。**诚实记录：Box/Mask 各涨了多少。**

---

## 第六步：导出 + 部署（交给板端的项目）

新模型训完、best.pt 拿到后，走部署流水线（这部分和现有 `rknn_seg_zc` 项目衔接，在 PC 上完成 ONNX 导出 + 图手术 + INT8 量化）：

1. 导出 ONNX：
```bash
yolo export model=runs/segment/seg_4500/weights/best.pt format=onnx simplify=True opset=12
```

2. cut-tail 图手术 + INT8 量化（用现有的 `convert_cut.py`，不改脚本，单类 cut-tail 逻辑通用）：
```bash
python convert_cut.py   # 输入 best.onnx → 输出 yolo8n_int8_cut_4500.rknn
```
（具体参数按现有脚本的来，单类不变。）

3. 推到板子（adb push）+ 回归测试：
```bash
# 在板子上跑一张测试图，看新模型的 conf/mask
./rknn_seg_zc image yolo8n_int8_cut_4500.rknn test_frame.png
```
新模型的 conf 会和旧模型不同（换了权重），重新跑一次 Python 参考（`post_cut.py`）对齐，确认 C++ 和 Python 一致即可。

4. **阈值可能要复核**：新模型训完，跑 val 的 PR 曲线，看 `OBJ_THRESH=0.18` 还合不合适。大概率不用大改，但如果新分布置信度整体变了，调这一个常量（`rknn_seg_zc.cpp:62`）即可，不动其他代码。

---

## 速查清单（给执行者）

- [ ] 盘点原始 3000 张：路径、nc/names、抽样看场景分布、确认 label 是 polygon 格式
- [ ] universe.roboflow.com 搜 `crack segmentation`，筛选 Instance Segmentation + 单类
- [ ] 下 1 个候选，抽查 data.yaml（单类）、label 格式（polygon）、图量、标注贴边质量
- [ ] 小批（1/3）混进 train，val 不动，用同超参训 100 epoch
- [ ] 看趋势：升→继续加全量；降→退回换候选；持平→可加可不加
- [ ] 全量混入，统一尺寸/类别id，重训 200 epoch
- [ ] best.pt → export onnx → convert_cut.py 量化 → adb push → 板端回归 + 阈值复核
- [ ] 诚实记录 final mAP（Box/Mask）对比 baseline

---

## 三个必须守住的底线

1. **val 集永远保持原始分布不动。** 它是你判断"新数据有没有用"的唯一标尺。混了新数据进 val，你就再也看不出"帮没帮上"了。
2. **每加一批，先小批量试，看 val mAP 升降再决定全量。** 降了就退。这是防止"加量反而变差"的唯一办法。
3. **数据格式必须核验。** Roboflow 偶尔有 bbox 误标成 seg、或 polygon 点稀疏的集。抽查 5-10 张 label + 图标注重叠，确认贴边、合理，再合。别下完直接合，会埋雷。

---

*执行者注意：这是数据扩充行动方案，执行环境是 PC（有 GPU 训练）。完成后把 best.pt / best.onnx 衔接到板端的 rknn_seg_zc 部署项目。训练侧和部署侧是解耦的，本方案不涉及板端代码改动（至多复核 1 个阈值常量）。*
